{
  "name": "example-project",
  "version": "1.0.0",
  "description": "Example project of simple blog REST API based on express",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "npm run start:pro",
    "start:pro": "dotenv -e .env.production nodemon src/server.js",
    "start:dev": "dotenv -e .env.development nodemon src/server.js",
    "test": "dotenv -e .env.test -- mocha --exit",
    "prisma:init": "npm run prisma:init:pro && npm run prisma:init:dev && npm run prisma:init:test",
    "prisma:init:pro": "dotenv -e .env.production -- prisma migrate dev --name init-pro",
    "prisma:init:dev": "dotenv -e .env.development -- prisma migrate dev --name init-dev",
    "prisma:init:test": "dotenv -e .env.test -- prisma migrate dev --name init-test",
    "fix": "npx prettier --write ."
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://gitlab.com/jan.havlena/example-project.git"
  },
  "author": "jan.havlena@profiq.com",
  "license": "MIT",
  "bugs": {
    "url": "https://gitlab.com/jan.havlena/example-project/issues"
  },
  "homepage": "https://gitlab.com/jan.havlena/example-project#readme",
  "dependencies": {
    "@prisma/client": "^4.0.0",
    "cors": "^2.8.5",
    "dotenv-cli": "^6.0.0",
    "express": "^4.18.1",
    "nodemon": "^2.0.19",
    "sqlite3": "^5.0.8"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "prettier": "^2.7.1",
    "prisma": "^4.0.0",
    "sequelize-cli": "^6.4.1",
    "supertest": "^6.2.4"
  }
}
